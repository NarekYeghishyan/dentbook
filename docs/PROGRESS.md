# Прогресс по шагам

Единственный источник истины по статусу. Порядок и критерии «Готово» — CLAUDE.md §10.
Шаг закрывается только после `pnpm lint && pnpm test` без падений.

Легенда: `[ ]` не начат · `[~]` в работе · `[x]` готово (критерий подтверждён)

**Все шаги плана закрыты** (Шаг 10 — 2026-09-18). Открыто: боевой деплой на VPS клиента и
ключи провайдеров (Q5, Шаги 6–8) — см. отметки `[ ]` в шагах.

**Тестовый стенд:** `https://dentbook.mashna.am` (с 2026-09-17), обновляется
`DEPLOY_HOST=dentbook-test deploy/deploy.sh` — см. [deploy/README.md](../deploy/README.md).
Прод-деплой — на VPS клиента, когда он появится (Q18): чек-лист в `deploy/README.md`.

---

## [x] Шаг 1 — каркас и БД

Монорепо, docker-compose с Postgres и Redis, схема в Drizzle, миграции, сиды.

**Готово:** `pnpm migrate` и `pnpm seed` проходят на чистой БД; ограничение `EXCLUDE`
подтверждено тестом на конкурентную вставку.

- [x] pnpm workspaces, структура `apps/*` + `packages/*` (CLAUDE.md §4)
- [x] `docker-compose.yml`: Postgres 16, Redis 7, healthchecks
- [x] базовые конфиги: tsconfig strict, ESLint, Prettier, Vitest, `.env.example`
- [x] каркас `apps/api`: zod-разбор окружения, request_id, редакция логов, `GET /health`
- [x] каркас `apps/worker`: подключение Redis для BullMQ, корректное завершение
- [x] `pnpm lint && pnpm typecheck && pnpm test` проходят на пустом проекте
- [x] `schema.sql` в корне репозитория — черновик, ADR-0003, 61 проверка на встроенном Postgres
- [x] согласование имён и решений в `schema.sql` (Q9), переключатель
      `clinics.booking_requires_confirmation`
- [x] схема Drizzle в `packages/db/src/schema`; паритет со `schema.sql` проверяется тестом
      (колонки, типы, значения по умолчанию, ограничения, индексы, расширения)
- [x] миграции: `0000_extensions` (кастомная) → `0001_init` → `0002_appointments_no_overlap`
      (кастомная: EXCLUDE §2.1 и gist-индекс исключений)
- [x] ограничение `appointments_no_dentist_overlap` (EXCLUDE USING gist) — §2.1
- [x] сиды `packages/db/src/seed`: демо-клиника, идемпотентны
- [x] интеграционный тест (Testcontainers, `postgres:16-alpine`): из 50 конкурентных
      записей на один слот проходит ровно одна, остальные — `23P01`
- [x] **критерий «Готово»:** `pnpm migrate` и `pnpm seed` прошли на чистой БД из
      docker compose; тест на конкурентную вставку проходит; `pnpm lint && pnpm test` зелёные.
      Тесты проверены на чувствительность: без EXCLUDE и при расхождении со `schema.sql` падают.

## [x] Шаг 2 — движок доступности

`packages/core`, чистые функции, unit-тесты.

**Готово:** покрытие ≥ 90%; тесты на буферы, исключения обоих типов, границы рабочих
часов, переход через полночь, переход на летнее время.

- [x] типы входа/выхода `computeSlots` (`packages/core/src/types.ts`)
- [x] `packages/core/src/availability.ts` — `computeSlots`, порядок операций по §6;
      `computeDaySlots` — слоты на местную дату (ADR-0005)
- [x] рабочие часы → UTC: `working-hours.ts` (ночные смены), `tz.ts` (Intl, DST)
- [x] объединение слотов нескольких врачей с дедупликацией по времени начала (`mergeSlots`)
- [x] выбор врача: минимальный `priority` → меньше записей за день → случайно (`pickDentist`)
- [x] тесты: буферы, `block`/`extra`, границы рабочих часов, полночь, DST (Нью-Йорк, Берлин,
      Лос-Анджелес, Ереван, Калькутта)
- [x] **критерий «Готово»:** 95 тестов; покрытие логики ядра 100% (порог 90% в
      `vitest.config.ts`); `pnpm lint && pnpm test` зелёные. Тесты проверены на
      чувствительность: без буфера записи, без ночной смены прошлого дня и с неверным
      выбором при осеннем переводе падают.

## [x] Шаг 3 — аутентификация и тенантность

**Готово:** тест на доступ к данным чужой клиники падает с 404/403 на каждом эндпоинте.

- [x] регистрация клиники с владельцем, вход, выход (`/v1/admin/auth/*`), `GET /v1/admin/me`
- [x] сессия — JWT в httpOnly-cookie, роль и активность из БД на каждом запросе (ADR-0006)
- [x] роли `owner` / `admin` / `registrar`, `config.roles` на роутах; сотрудники
      (`/v1/admin/users`) и настройки клиники (`/v1/admin/clinic`)
- [x] middleware проверки `clinic_id` (§2.2): хук `authenticate` на всей области `/v1/admin`
      кроме `/auth`, `clinicId` только из сессии
- [x] zod-схемы входа в `packages/shared` (§9), единый формат ошибок, ошибки БД в логах без ПДн
- [x] лимит попыток входа/регистрации по IP
- [x] тест изоляции тенантов на каждый эндпоинт: роуты собираются из приложения, роут без
      проверки роняет тест
- [x] **критерий «Готово»:** для каждого роута `/v1/admin` проверено, что сотрудник чужой
      клиники получает 404 или только свои данные; без сессии — 401; registrar на закрытых
      роутах — 403. `pnpm lint && pnpm test` зелёные (166 тестов). Тест проверен на
      чувствительность: без фильтра по `clinic_id` падает.

## [x] Шаг 4 — админский API и панель

**Готово:** клиника может с нуля завести данные и увидеть корректный календарь доступности.

- [x] API: филиалы, услуги, врачи, услуги врача, недельный шаблон, исключения
      (`/v1/admin/locations|services|dentists…`); удаления нет — отключение (ADR-0007)
- [x] смены врача не пересекаются даже в разных филиалах (`findWeeklyOverlap`, core)
- [x] закрыть время с записями нельзя (§8): `409 slot_taken` + `conflicts`
- [x] приоритет врачей перетаскиванием (`PUT /dentists/order`, dnd-kit)
- [x] календарь доступности `GET /v1/admin/availability` — общий сервис с будущим публичным
      API; учтены пояс филиала, `min_lead_min`, `max_advance_days`, живые холды
- [x] панель `apps/admin`: вход, регистрация, календарь с чек-листом первых шагов, врачи,
      карточка врача, услуги, офисы, сотрудники, настройки; en/ru/hy; регистратура — только
      просмотр
- [x] панель раздаётся API на `/admin/` (Q14), сборка в `Dockerfile`
- [x] тест изоляции тенантов расширен на все новые роуты (20)
- [x] **критерий «Готово»:** в браузере (Playwright, локально) клиника с нуля
      зарегистрировалась, завела офис, услугу, двух врачей со сменами, поменяла приоритет
      перетаскиванием и увидела в календаре 155 слотов недели с врачами в новом порядке;
      интеграционные тесты календаря — рабочий день, приоритет, записи и блоки, extra в
      выходной, минимальный запас, границы дат, переход на зимнее время.
      `pnpm lint && pnpm test` зелёные (242 теста).

## [x] Шаг 5 — ключи и публичный API

**Готово:** холд истекает и слот возвращается; конкурентное подтверждение одного слота
даёт ровно одну запись.

- [x] буфер после приёма защищён в БД (Q11): `blocked_until`, EXCLUDE по нему, §2.1 обновлён
- [x] выдача, правка и отзыв `pk_*` (`/v1/admin/api-keys`), страница «Сайт» в панели
- [x] проверка `Origin` против `allowed_origins` (§2.5), CORS и preflight
- [x] публичный API §7: config, services, availability, holds, verifications, appointments,
      статус и отмена по токену; контракт snake_case (ADR-0008)
- [x] лимиты: 60/мин на ключ, 10/мин холдов и SMS с IP, 5 кодов в час на номер (Redis)
- [x] холды: назначение по §6, advisory-lock против блоков, 23P01 → следующий врач или
      `slot_taken` + альтернативы; истечение — на лету и задачей worker'а раз в минуту
- [x] SMS-код: HMAC, 5 мин, 3 попытки; провайдер — Шаг 6 (до него `503`)
- [x] кеш доступности в Redis (§6, TTL 60 с) со сбросом по врачу и по клинике
- [x] тест изоляции тенантов для каждого публичного роута и для роутов ключей
- [x] **критерий «Готово»:** истёкший холд не мешает новому на это время, слот снова в
      выдаче; 20 одновременных холдов одного слота → ровно 2 (по числу свободных врачей),
      остальные `slot_taken`; два одновременных подтверждения одного холда → ровно одна
      запись. `pnpm lint && pnpm test` зелёные (288 тестов).

## [x] Шаг 6 — виджет

**Готово:** запись создаётся end-to-end на тестовой странице; бандл < 50 КБ gzip.

- [x] форма `apps/widget`: услуга → офис → день и время → данные → SMS-код → готово,
      отмена записи, альтернативы при `slot_taken`, обратный отсчёт холда (ADR-0009)
- [x] Shadow DOM, цвет клиники, en/ru/hy, телефон → E.164, адаптивность (375 px)
- [x] капча Cloudflare Turnstile: сервер проверяет токен до SMS; включается ключами
- [x] SMS через Twilio (`TwilioSmsSender`); без ключей — 503 и понятный текст в форме
- [x] бандл раздаёт API на `/widget/`; код встраивания — на странице «Сайт» панели
- [x] тестовая страница `apps/widget/test-page`; `pnpm test:e2e` — Playwright + Chromium
- [x] проверка размера: `pnpm --filter @dentbook/widget size` (8.2 КБ gzip из 50)
- [x] **критерий «Готово»:** e2e-тест в Chromium — клиент на странице «сайта клиники»
      (другой origin) выбирает услугу и время, вводит телефон и код из SMS и получает
      подтверждённую запись в БД; стили сайта не протекают в форму; на 375 px нет
      горизонтальной прокрутки; бандл 8.2 КБ gzip. `pnpm lint && pnpm test` (310 тестов)
      и `pnpm test:e2e` (5) зелёные.
- [ ] на `https://mashna.am` запись дойдёт до конца, когда будут ключи Twilio и Turnstile
      (Q5); до них форма работает до ввода телефона

## [x] Шаг 7 — бот и Mini App

**Готово:** врач проходит весь цикл в Telegram без админки.

- [x] привязка по одноразовому токену, TTL 24 ч, в БД только хеш; в карточке врача —
      статус, «Копировать ссылку», QR-код, отключение (Q15, ADR-0010)
- [x] webhook с секретным заголовком + дедупликация по `update_id` (`telegram_updates`)
- [x] исходящие сообщения только через очередь BullMQ `telegram` с backoff; 403 →
      `telegram_blocked`, без повторов
- [x] алерты: новая запись (кнопки «Открыть расписание» и «Подтвердить»), отмена клиентом,
      напоминание о неподтверждённой записи через 2 ч (Q12)
- [x] `apps/api/src/telegram/verify-init-data.ts` + тест на подделанную подпись и
      просроченную `auth_date`
- [x] Mini App `apps/miniapp` на `/miniapp/`: расписание дня с подтверждением, закрытие и
      открытие времени, запись своего клиента; en/ru/hy, тема Telegram
- [x] закрыть время с записями нельзя (§8): список записей, перенос через регистратуру
- [x] настройка бота: `apps/api/src/telegram-setup.ts` (вебхук, кнопка меню, команды)
- [x] тест изоляции: врач видит и меняет только своё, чужая клиника — 404
- [x] **критерий «Готово»:** интеграционные тесты бота — привязка по `/start <токен>`,
      алерт с кнопкой, подтверждение кнопкой, напоминание, 403; e2e в Chromium
      (`miniapp.e2e.test.ts`) — врач в Mini App подтверждает запись с сайта, получает отказ
      при закрытии времени поверх записи, закрывает свободное время, записывает своего
      клиента (запись `confirmed`, `source = telegram`) и открывает время обратно.
      `pnpm lint && pnpm test` (363 теста) и `pnpm test:e2e` (10) зелёные.
- [ ] проверка на стенде с настоящим ботом — после токена от @BotFather в
      `/opt/dentbook/.env` и запуска `telegram-setup.ts`

## [x] Шаг 8 — уведомления

**Готово:** напоминания ставятся и снимаются, повторные попытки работают.

- [x] SMS-провайдер Twilio в `@dentbook/shared/sms`: коды — из API сразу, уведомления —
      из worker'а через очередь `sms` (ADR-0011)
- [x] шаблоны en/ru/hy в `apps/worker/src/i18n`: подтверждение записи (Q12), напоминания;
      текст собирается в момент отправки, в очереди только id уведомления (§2.6)
- [x] напоминания за 24 ч и 2 ч при записи с сайта и врачом в Mini App; прошедшие не
      ставятся; перед отправкой — проверка, что запись подтверждена и не началась
- [x] снятие при отмене клиентом: строки `cancelled`, задачи удаляются из очереди
- [x] повторы: 5 попыток с нарастающей паузой; неверный номер или отписка → сразу `failed`
- [x] **критерий «Готово»:** запись ставит два напоминания на точное время (с учётом
      перехода на зимнее время), отмена снимает оба и удаляет задачи из очереди (BullMQ на
      Redis: отложенная задача появляется под id уведомления и исчезает после снятия);
      настоящий BullMQ-worker при ответах провайдера 503 и 429 повторяет отправку и на
      третьей попытке отмечает SMS отправленным; постоянный отказ — `failed` без
      повторов. `pnpm lint && pnpm test` (390 тестов) и `pnpm test:e2e` (10) зелёные.
- [x] тихие часы 21:00–8:00 для напоминаний и строка об отписке (Q16)
- [ ] на стенде — после ключей Twilio (Q5)

## [x] Шаг 9 — журнал и отчёты

**Готово:** перенос записи мышью не создаёт пересечений.

- [x] журнал `/v1/admin/journal` и страница «Журнал»: день — колонки врачей офиса,
      неделя — дни врача; рабочее и закрытое время на сетке по часам офиса (ADR-0012)
- [x] drag-and-drop (dnd-kit): по времени с шагом клиники и к другому врачу; пересечения
      отклоняет EXCLUDE, закрытое время — проверка под lock врача
- [x] регистратура (Q17): запись клиента щелчком по свободному времени, подтверждение,
      отмена клиникой (SMS клиенту), «пришёл» / «не пришёл»
- [x] уведомления о переносе: врачам — в Telegram, клиенту — SMS и новые напоминания
- [x] клиенты: поиск по имени и телефону, карточка с заметками, счётчиками и историей
- [x] выгрузка CSV (владелец и администратор): пояс офиса, BOM, защита от формул
- [x] дашборд: записи по источникам, отмены, неявки, загрузка врачей, неподтверждённые
- [x] тест изоляции тенантов на все новые роуты (9)
- [x] **критерий «Готово»:** 8 одновременных переносов разных записей на одно время к
      одному врачу → ровно один успешный, остальные 409 `slot_taken`; e2e в Chromium —
      перенос мышью по времени и к другому врачу, перенос на занятое время отклонён и
      запись осталась на месте, у врачей нет двух записей на одно время.
      `pnpm lint && pnpm test` (434 теста) и `pnpm test:e2e` (12) зелёные.

## [x] Шаг 10 — сдача

**Готово:** 50 одновременных попыток записи на один слот дают ровно одну запись.

- [x] панель оператора (Q18, ADR-0013): клиники со сводкой, приостановка и возобновление,
      состояние платформы (провайдеры, очереди, ошибки доставки за сутки); вход общий,
      `POST /auth/login` отвечает ролью; оператор создаётся командой на сервере
- [x] приостановленная клиника: форма, панель, Mini App и бот не работают, визиты и
      напоминания остаются
- [x] нагрузочный тест: инструмент `apps/api/src/load-test.ts` (против любого
      развёртывания) и `load.integration.test.ts` (настоящий HTTP-сервер)
- [x] документация: [публичный API](api/public-api.md), [встраивание формы](embed.md)
      (на английском, для клиник)
- [x] эксплуатация: `deploy/backup.sh` с ротацией, ротация логов контейнеров, чек-лист
      боевого запуска в `deploy/README.md`
- [x] **критерий «Готово»:** 50 одновременных `POST /holds` одного слота по HTTP → 1 холд,
      49 `slot_taken`; 50 одновременных подтверждений выигравшего холда → ровно одна
      запись в БД. На стенде тот же инструмент: один холд, остальные — `slot_taken` и 429
      по лимиту IP. `pnpm lint && pnpm test` (444 теста) и `pnpm test:e2e` (12) зелёные.
- [ ] боевой деплой — когда появится VPS клиента (Q18): `deploy/README.md`, «Боевой запуск»
